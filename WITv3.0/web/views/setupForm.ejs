<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bot Setup Form</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: #1e293b;
            color: #e2e8f0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }
        .container {
            width: 100%;
            max-width: 800px;
            padding: 2.5rem;
            border-radius: 0.75rem;
            background-color: #334155;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            border: 1px solid #475569;
        }
        h1, h2 {
            text-align: center;
            color: #f8fafc;
        }
        h1 {
            font-size: 1.875rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        h2 {
            font-size: 1.25rem;
            font-weight: 500;
            margin-top: 2rem;
            margin-bottom: 1.5rem;
            border-bottom: 1px solid #475569;
            padding-bottom: 0.5rem;
        }
        .form-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }
        @media (min-width: 640px) {
            .form-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }
        .form-group {
            margin-bottom: 0;
        }
        .full-width {
            grid-column: 1 / -1;
        }
        label {
            display: block;
            margin-bottom: 0.5rem;
            font-size: 0.875rem;
            font-weight: 500;
            color: #cbd5e1;
        }
        input[type="text"], input[type="url"], textarea {
            width: 100%;
            padding: 0.75rem 1rem;
            border-radius: 0.375rem;
            background-color: #1e293b;
            border: 1px solid #475569;
            color: #e2e8f0;
            font-size: 1rem;
            box-sizing: border-box;
        }
        input:focus, textarea:focus {
            outline: none;
            border-color: #38bdf8;
            box-shadow: 0 0 0 2px rgba(56, 189, 248, 0.4);
        }
        textarea {
            min-height: 120px;
            font-family: monospace;
        }
        button {
            width: 100%;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 0.375rem;
            background-color: #0ea5e9;
            color: #ffffff;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s ease-in-out;
            margin-top: 2.5rem;
        }
        button:hover { background-color: #38bdf8; }
        small {
            font-size: 0.8rem;
            color: #94a3b8;
            margin-top: 0.25rem;
            display: block;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Bot Initial Configuration</h1>
        <form action="/setup/<%= token %>" method="POST">

            <h2>Role Configuration</h2>
            <div class="form-grid">
                <div class="form-group full-width">
                    <label for="adminRoles">Admin Roles</label>
                    <input type="text" id="adminRoles" name="adminRoles" required value="<%= currentConfig.adminRoles || '' %>">
                    <small>Comma-separated list of Role IDs.</small>
                </div>
                <div class="form-group">
                    <label for="councilRoles">Council Roles</label>
                    <input type="text" id="councilRoles" name="councilRoles" required value="<%= currentConfig.councilRoles || '' %>">
                     <small>Comma-separated list of Role IDs.</small>
                </div>
                <div class="form-group">
                    <label for="commanderRoles">Commander Roles</label>
                    <input type="text" id="commanderRoles" name="commanderRoles" required value="<%= currentConfig.commanderRoles || '' %>">
                     <small>Comma-separated list of Role IDs.</small>
                </div>
                 <div class="form-group">
                    <label for="authRoles">Auth Roles</label>
                    <input type="text" id="authRoles" name="authRoles" required value="<%= currentConfig.authRoles || '' %>">
                    <small>Comma-separated list of Role IDs that can use /auth.</small>
                </div>
                <div class="form-group">
                    <label for="removeAllRoles">"Remove All" Roles</label>
                    <input type="text" id="removeAllRoles" name="removeAllRoles" value="<%= currentConfig.removeAllRoles || '' %>">
                    <small>Optional: Role IDs that SHOULD be removed by the /demote "Remove All" command.</small>
                </div>
            </div>

            <h2>Channel Configuration</h2>
             <div class="form-grid">
                <div class="form-group">
                    <label for="requestChannelId">Request Channel ID</label>
                    <input type="text" id="requestChannelId" name="requestChannelId" required value="<%= currentConfig.requestChannelId || '' %>">
                </div>
                <div class="form-group">
                    <label for="archiveChannelId">Archive Channel ID</label>
                    <input type="text" id="archiveChannelId" name="archiveChannelId" required value="<%= currentConfig.archiveChannelId || '' %>">
                </div>
                <div class="form-group">
                    <label for="auditLogChannelId">Audit Log Channel ID</label>
                    <input type="text" id="auditLogChannelId" name="auditLogChannelId" required value="<%= currentConfig.auditLogChannelId || '' %>">
                </div>
                 <div class="form-group">
                    <label for="actionLogChannelId">Action Log Channel ID</label>
                    <input type="text" id="actionLogChannelId" name="actionLogChannelId" required value="<%= currentConfig.actionLogChannelId || '' %>">
                </div>
            </div>
            
            <h2>EVE Integration</h2>
            <div class="form-grid">
                <div class="form-group">
                    <label for="incursionChannelId">Incursion Channel ID</label>
                    <input type="text" id="incursionChannelId" name="incursionChannelId" required value="<%= currentConfig.incursionChannelId || '' %>">
                </div>
                 <div class="form-group">
                    <label for="srpChannelId">SRP Channel ID</label>
                    <input type="text" id="srpChannelId" name="srpChannelId" required value="<%= currentConfig.srpChannelId || '' %>">
                </div>
                 <div class="form-group">
                    <label for="srpMailingListId">SRP Mailing List ID</label>
                    <input type="text" id="srpMailingListId" name="srpMailingListId" required value="<%= currentConfig.srpMailingListId || '' %>">
                </div>
                 <div class="form-group">
                    <label for="residentAppChannelId">Resident Application Channel ID</label>
                    <input type="text" id="residentAppChannelId" name="residentAppChannelId" required value="<%= currentConfig.residentAppChannelId || '' %>">
                </div>
                 <div class="form-group">
                    <label for="logiSignoffChannelId">Logi Signoff Channel ID</label>
                    <input type="text" id="logiSignoffChannelId" name="logiSignoffChannelId" required value="<%= currentConfig.logiSignoffChannelId || '' %>">
                </div>
            </div>

            <h2>GitHub Integration</h2>
            <div class="form-grid">
                <div class="form-group full-width">
                    <label for="githubRepoUrl">GitHub Repository URL</label>
                    <input type="url" id="githubRepoUrl" name="githubRepoUrl" value="<%= currentConfig.githubRepoUrl || '' %>" placeholder="e.g., https://github.com/user/repo">
                </div>
                <div class="form-group">
                    <label for="githubBranch">Branch to Watch</label>
                    <input type="text" id="githubBranch" name="githubBranch" value="<%= currentConfig.githubBranch || '' %>" placeholder="e.g., main">
                </div>
                <div class="form-group">
                    <label for="githubChannelId">GitHub Notification Channel ID</label>
                    <input type="text" id="githubChannelId" name="githubChannelId" value="<%= currentConfig.githubChannelId || '' %>">
                </div>
            </div>

            <button type="submit">Save Configuration</button>
        </form>
    </div>
</body>
</html>
